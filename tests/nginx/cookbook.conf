server {
    listen 80;
    listen [::]:80;

    server_name localhost;

    root /home/cookbook;

    location / {
        try_files $uri $uri/ =404;
    }

    location /recipes {
        include /etc/nginx/uwsgi_params;
        uwsgi_pass unix:/var/run/cookbook.sock;
    }
}